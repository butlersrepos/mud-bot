import {expect, should} from 'chai';
import rewire from 'rewire';
let underTest = rewire('../build/memory-organ');

describe(`Memory Organ`, () => {
	describe(`#remember`, () => {
		it('should read HP info and remember it', () => {

		});
	});
});
